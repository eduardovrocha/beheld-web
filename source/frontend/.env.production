# Vite carrega este arquivo automaticamente em `vite build` (NODE_ENV=production).
# Same-origin: o SPA é servido por Caddy no mesmo host do Rails — paths
# relativos como `/api` resolvem via reverse-proxy do Caddyfile.

VITE_API_URL=/api
