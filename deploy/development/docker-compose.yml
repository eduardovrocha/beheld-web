# Development orchestration for beheld (Rails 7.2 API + React SPA).
#
# Postgres and Redis are expected to be running on the HOST (not in containers).
# The `host.docker.internal:host-gateway` mapping makes that work on:
#   - Docker Desktop (macOS / Windows)      — already resolves out of the box
#   - Linux Docker 20.10+                    — needs the host-gateway alias below
#
# Override defaults via a local `.env` (copy from `.env.example`).

# Explicit project name — without this, Compose derives the project name from
# the parent directory ("development"), which collides with any other project
# at <something>/deploy/development/.
name: beheld

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: beheld-backend:dev
    container_name: beheld-backend-dev
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    volumes:
      - ../../source/backend:/app
      - bundle-cache:/usr/local/bundle
      - rails-tmp:/app/tmp
    environment:
      RAILS_ENV: development
      RAILS_LOG_TO_STDOUT: "true"
      BINDING: "0.0.0.0"
      # Postgres on the host
      DB_HOST: ${DB_HOST:-host.docker.internal}
      DB_PORT: ${DB_PORT:-5432}
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-beheld_backend_development}
      DB_NAME_TEST: ${DB_NAME_TEST:-beheld_backend_test}
      # Redis on the host
      REDIS_URL: ${REDIS_URL:-redis://host.docker.internal:6379/1}
      # CORS — comma-separated list, "*" for dev
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      # Portal público (obrigatória — Rails lança KeyError se não setada).
      # Em dev aponta ao SPA Vite; em prod ao host público (beheld.dev).
      PORTAL_PUBLIC_URL: ${PORTAL_PUBLIC_URL:-http://localhost:5173}
      # GitHub OAuth (Phase 5 / F5.6) — values come from local .env, never
      # baked into the image. Empty defaults so the container still boots if
      # the developer hasn't filled them in yet.
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      GITHUB_OAUTH_CLIENT_SECRET: ${GITHUB_OAUTH_CLIENT_SECRET:-}
      # Platform signing key (Phase 5 / F5.6.1) — used to sign identity
      # attestations. The private key NEVER leaves the host environment;
      # it's loaded as a base64-encoded raw 32-byte seed.
      BEHELD_PLATFORM_KEY_ID: ${BEHELD_PLATFORM_KEY_ID:-}
      BEHELD_PLATFORM_PRIVATE_KEY: ${BEHELD_PLATFORM_PRIVATE_KEY:-}
    tty: true
    stdin_open: true
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend.dev
    image: beheld-frontend:dev
    container_name: beheld-frontend-dev
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    volumes:
      - ../../source/frontend:/app
      - frontend-node-modules:/app/node_modules
    environment:
      # API base the SPA hits at runtime (from the BROWSER, hence localhost
      # rather than the docker service name).
      VITE_API_URL: ${VITE_API_URL:-http://localhost:${BACKEND_PORT:-3000}}
    tty: true
    stdin_open: true
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  bundle-cache:
    name: beheld-backend-bundle-cache
  rails-tmp:
    name: beheld-backend-rails-tmp
  frontend-node-modules:
    name: beheld-frontend-node-modules
